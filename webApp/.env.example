# Copiar a .env y ajustar si hace falta. .env no se sube a git.

# Google Tag Manager — contenedor web (WebApp). Si está definido, la app usa GTM en lugar de gtag directo.
VITE_GTM_CONTAINER_ID=GTM-WLXN93VK

# Opcional: GA4 solo se usa cuando NO hay VITE_GTM_CONTAINER_ID
# VITE_GA4_MEASUREMENT_ID=G-BMZEQNRKR4
