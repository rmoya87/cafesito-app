CREATE TABLE coffee_cache (
  id TEXT NOT NULL PRIMARY KEY,
  name TEXT NOT NULL,
  origin TEXT NOT NULL
);

CREATE INDEX coffee_cache_name_idx ON coffee_cache(name);

ALTER TABLE coffee_cache ADD COLUMN updated_at INTEGER NOT NULL DEFAULT 0;
